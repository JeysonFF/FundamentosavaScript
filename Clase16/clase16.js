var signo = prompt('¿Cúal es tu signo?');


switch(signo){
    case 'acuario':
        console.log(`${signo}`)
        break;
    case 'picis':
        console.log(`${signo}`)
        break;
    default:
        console.log(`el signo "${signo}", no existe`)
}
